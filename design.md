# Super SCM 디자인 시스템

> 월간 발주계획 시스템의 모든 화면에 일관되게 적용하는 시각·상호작용 기준입니다.
> 기준 방향은 첨부된 Nexus SCM AI 화면의 `Enterprise Intelligence` 분위기를 한국후지필름BI의 SCM 업무에 맞게 정리한 것입니다.

## 1. 디자인 방향

### 핵심 인상

- **정확함**: 숫자, 단위, 기준일, 상태를 한눈에 확인할 수 있어야 합니다.
- **신뢰감**: 과한 장식보다 여백, 정돈된 표, 명확한 경계선을 우선합니다.
- **업무 집중**: 사용자가 다음 판단과 행동을 바로 알 수 있도록 화면의 위계를 분명히 합니다.
- **운영 친화성**: 정상, 주의, 위험, 확인 불가를 색상만이 아니라 문구와 상태 배지로 함께 전달합니다.

### 시각적 키워드

`Enterprise Intelligence` · `Calm Precision` · `Operational Clarity`

밝은 캔버스 위에 딥 네이비 내비게이션을 두고, 주요 행동은 블루로 강조합니다. 카드는 흰색과 얇은 회색 테두리를 기본으로 하며, 그림자는 떠 있는 느낌보다 계층을 구분하는 정도로만 사용합니다.

## 2. 적용 범위와 원칙

- 스타일은 **순수 CSS**로 구현합니다. Tailwind, styled-components, CSS Modules를 추가하지 않습니다.
- 공통 토큰과 컴포넌트 스타일은 `app/globals.css`에 둡니다.
- 화면은 기존 공통 클래스(`app-shell`, `sidebar`, `topbar`, `content`, `card`, `grid`, `tag`, `button`)를 우선 사용합니다.
- 분석 화면은 `analysis-page`, `analysis-heading`, `analysis-table-wrap`, `analysis-table`을 우선 사용합니다.
- 화면별 임의 색상값을 만들지 않습니다. 필요한 색상은 토큰으로 먼저 추가합니다.
- 수치와 상태의 의미를 색상만으로 전달하지 않습니다. 값, 단위, 텍스트, 배지를 함께 표시합니다.
- 업무용 화면의 장식은 정보 이해에 기여할 때만 사용합니다.

## 3. 색상 토큰

현재 `app/globals.css`의 토큰을 기준으로 하며, 새 화면에서도 아래 역할을 지킵니다.

| 역할 | 토큰 | 기본값 | 사용처 |
|---|---|---:|---|
| 기본 글자 | `--ink` | `#182230` | 제목, 본문, 표 값 |
| 보조 글자 | `--muted` | `#718096` | 설명, 단위, 보조 정보 |
| 캔버스 | `--canvas` | `#f5f7fb` | 페이지 배경 |
| 표면 | `--surface` | `#ffffff` | 카드, 패널, 입력 영역 |
| 내비게이션 | `--navy` | `#17365d` | 브랜드, 주요 제목, 딥 네이비 영역 |
| 주요 액션 | `--blue` | `#2f6fed` | 활성 메뉴, 기본 버튼, 링크, 포커스 |
| 주요 액션 배경 | `--pale-blue` | `#edf4ff` | 선택 상태, 안내 박스, 아이콘 배경 |
| 정상 | `--green` | `#1f9d68` | 완료, 안전, 개선, 유효한 확정 |
| 정상 배경 | `--pale-green` | `#eaf8f1` | 정상 배지 |
| 주의 | `--amber` | `#bd7211` | 표본 부족, 검토 필요, 경고 |
| 주의 배경 | `--pale-amber` | `#fff6e4` | 주의 배지와 콜아웃 |
| 위험 | `--red` | `#c2413d` | 초과, 긴급, 소진 위험, 실패 |
| 위험 배경 | `--pale-red` | `#fff0ef` | 위험 배지와 오류 콜아웃 |
| 경계선 | `--line` | `#e3e8ef` | 카드, 표, 입력, 구분선 |

### 상태 색상 규칙

- `SAFE`, 완료, 유효한 확정값: `tag green`, `text-good`
- 검토 필요, 표본 부족, 진행 중: `tag amber`, `callout`
- `CRITICAL`, 기준 초과, 조회 실패: `tag red`, `text-danger`
- `UNKNOWN`, 계산 불가, 미확정: `tag gray`와 사유 문구
- 양수/음수는 도메인 의미에 따라 판단합니다. 예를 들어 리드타임 격차가 양수이면 위험이므로 빨간색입니다.
- `null`은 0이나 임의의 큰 숫자로 대체하지 않고 `—`와 사유를 표시합니다.

## 4. 타이포그래피

- 기본 글꼴: `Noto Sans KR`, 대체 글꼴은 `ui-sans-serif`, `system-ui`, `sans-serif`입니다.
- 한국어 본문은 읽기 편한 중간 굵기와 충분한 행간을 사용합니다.
- 페이지 제목은 25~28px, 굵기 800, 자간은 약간 좁게 설정합니다.
- 카드 제목은 14~16px, 굵기 700입니다.
- 본문과 표는 12~14px을 기본으로 합니다.
- 레이블, 섹션 킥커, 상태 설명은 10~11px과 넓은 자간을 사용합니다.
- 숫자·날짜·코드가 반복되는 영역은 가능하면 모노스페이스 계열을 사용해 열 정렬을 돕습니다.
- 단위는 숫자와 붙여 쓰되, 긴 설명 안에서는 숫자와 단위를 시각적으로 구분합니다. 예: `18.0일`, `20건`.

## 5. 레이아웃

### 데스크톱

- 전체 구조는 `app-shell → sidebar + main`입니다.
- 사이드바 폭은 약 240~250px로 고정하고, 본문은 남은 영역을 사용합니다.
- `topbar`는 화면 상단의 컨텍스트와 사용자 도구를 담당합니다.
- `content`는 최대 1440px, 분석 화면은 최대 1280px을 기본으로 합니다.
- 페이지 좌우 여백은 넓은 화면에서 32~42px, 태블릿에서 26px을 사용합니다.
- 한 화면에는 핵심 KPI → 주요 분석/표 → 보조 설명/행동 순서로 배치합니다.

### 그리드

- `grid-4`: KPI 4개 또는 요약 카드 4개
- `grid-3`: 분석 KPI 3개
- `grid-2`: 비교 패널, 입력과 결과, 두 개의 주요 섹션
- 열 사이 간격은 15~18px을 기본으로 합니다.
- 카드 안의 정보는 한 카드에 하나의 판단 목적만 담습니다.

### 반응형

- 1050px 이하에서는 사이드바와 여백을 줄이고 4열 KPI를 2열로 바꿉니다.
- 760px 이하에서는 사이드바를 상단 내비게이션으로 바꾸고 모든 그리드를 1열로 쌓습니다.
- 표는 줄바꿈으로 열을 찌그러뜨리지 않고 가로 스크롤을 허용합니다.
- 모바일에서 버튼은 최소 44px 높이의 터치 영역을 확보합니다.
- 긴 제목과 설명은 자연스럽게 줄바꿈하되, 코드·수치·배지는 가능한 한 한 줄을 유지합니다.

## 6. 공통 컴포넌트

### 사이드바와 브랜드

- 배경은 딥 네이비 계열로 유지합니다.
- 브랜드 마크는 블루 사각형 또는 둥근 사각형 안의 단순한 기하 형태를 사용합니다.
- 브랜드명은 `한국후지필름BI SCM`처럼 실제 제품명을 우선하며, 영문 부제는 선택 사항입니다.
- 메뉴는 아이콘/번호와 한국어 라벨을 함께 사용합니다.
- 활성 메뉴는 블루 배경과 흰색 글자로 명확히 표시합니다.
- 메뉴 그룹에는 짧은 섹션 라벨을 사용할 수 있으나 메뉴가 많아 보이게 만들지 않습니다.

### 상단 바

- 왼쪽에는 현재 업무 단계 또는 페이지 제목을 둡니다.
- 오른쪽에는 기준일, 데이터 상태, 사용자 도구를 둡니다.
- 고정 헤더를 사용할 경우 본문을 가리지 않도록 배경과 아래 경계선을 둡니다.
- 알림·도움말·설정은 아이콘만 사용할 때도 `aria-label`을 제공합니다.

### 페이지 헤더

- 작은 `eyebrow` 또는 업무 영역 라벨 → 제목 → 한 문장 설명 순서입니다.
- 제목은 사용자가 이 화면에서 확인하는 대상을 명사형으로 명확히 씁니다.
- 설명에는 계산의 목적과 기준을 짧게 씁니다.
- 우측에는 다운로드, 확정, 다음 단계 같은 1~2개의 핵심 행동만 둡니다.

### 카드

- 기본 카드: 흰색 배경, `1px solid var(--line)`, 14px 안팎의 모서리, 18px 안팎의 내부 여백
- 카드 제목은 왼쪽, 기간·단위·기준은 오른쪽에 둡니다.
- 카드에 그림자를 넣을 때는 매우 약하게 사용합니다.
- 강조 카드에는 전체 테두리보다 상단 3~4px 컬러 라인을 우선합니다.
- 한 카드 안에 서로 다른 목적의 표·입력·행동을 과도하게 섞지 않습니다.

### KPI

- `metric-label` → `metric-value` → `metric-foot` 순서를 지킵니다.
- 큰 숫자만 보여주지 말고 단위와 집계 범위를 함께 보여줍니다.
- KPI 숫자를 화면에서 재계산하지 않습니다. SQL 또는 조회 모델이 제공한 값을 그대로 표시합니다.
- 계산 불가 건수는 별도 KPI로 분리할 수 있으며, 원인을 `NO_USAGE`, `NO_LEADTIME`처럼 설명합니다.

### 표

- 표는 업무 판단의 중심으로 취급하며, 장식보다 열 정렬과 스캔성을 우선합니다.
- 헤더는 연한 회색 배경과 진한 보조 글자로 구분합니다.
- 숫자·날짜·수량·일수는 오른쪽 정렬, 상태는 가운데 또는 왼쪽 정렬을 기본으로 합니다.
- 행 높이는 40~48px 정도로 유지하고, 행 호버는 아주 옅은 배경색만 사용합니다.
- 표가 비어 있으면 “표시할 데이터가 없습니다”와 함께 확인할 뷰/설정을 안내할 수 있습니다.
- 조회 실패와 빈 결과를 같은 화면으로 처리하지 않습니다.
- 행 수가 많은 경우 페이지네이션, 필터, 정렬 기준을 함께 제공하되 한 번에 모두 노출하지 않습니다.

### 배지

- 상태 배지는 `tag`를 사용하고 한두 단어의 짧은 라벨을 넣습니다.
- 배지는 색상·라벨·필요 시 아이콘으로 상태를 전달합니다.
- 정상/주의/위험/미확정의 의미를 화면마다 바꾸지 않습니다.
- 문장 전체를 배지 안에 넣지 않습니다. 설명은 배지 바깥에 둡니다.

### 버튼

- 한 화면의 가장 중요한 행동은 `button primary` 하나로 제한합니다.
- 보조 행동은 기본 `button`, 되돌리기 쉬운 보조 선택은 `button ghost`를 사용합니다.
- 위험한 확정·삭제 행동은 빨간색으로 꾸미기보다 확인 문구와 명확한 버튼 라벨을 우선합니다.
- 버튼 라벨은 `저장`, `확정`, `다음 단계`, `CSV 다운로드`처럼 결과가 분명한 동사형을 사용합니다.
- 처리 중에는 버튼을 비활성화하고 현재 상태를 알려 중복 실행을 막습니다.

### 입력과 시나리오 조정

- 입력 라벨은 항상 입력 위에 두고, 단위·허용 범위·기준값을 인접하게 표시합니다.
- 슬라이더는 현재값, 최소값, 최대값을 모두 보여줍니다.
- 변경 결과는 “기준 대비 변화”와 “적용 전/후”를 분리해서 보여줍니다.
- 사용자가 바꾼 값은 임시 상태로 두고, 실제 계획 반영은 별도 확정 행동으로 구분합니다.

## 7. 데이터·상태 화면

### 로딩

- 화면 구조를 유지한 채 카드와 표의 자리 표시 영역을 보여줍니다.
- 로딩 중에는 “계산 중입니다” 또는 “데이터를 불러오는 중입니다”를 표시합니다.

### 조회 실패

- 제목: `조회에 실패했습니다.`
- 설명: 사용자가 할 수 있는 확인 방법을 짧게 안내합니다.
- 개발자용 오류 문자열은 보조 텍스트로 표시하되, 민감한 키나 연결 정보를 노출하지 않습니다.

### 빈 결과

- 제목: `표시할 데이터가 없습니다.`
- 가능한 경우 `analytics` 뷰, Exposed schemas, 기준일 필터를 확인하도록 안내합니다.
- 빈 결과를 정상 상태로 꾸미거나 임의의 0건 숫자로 오해하게 만들지 않습니다.

### 계산 불가

- 값을 `999`, `0`, 빈 문자열로 대체하지 않습니다.
- `—`와 함께 `사용 이력 없음`, `리드타임 미확정` 같은 원인을 표시합니다.
- 모델의 `reason`을 화면 문구로 변환하는 규칙은 공통화합니다.

## 8. 차트와 시각화

- 차트 라이브러리를 추가하지 않습니다. 필요한 경우 CSS/SVG 기반의 단순한 시각화를 사용합니다.
- 실제값과 예측값은 선의 색과 범례를 함께 사용합니다.
- 신뢰구간은 옅은 블루 영역으로 표현하며, 중앙 예측선보다 시각적 강도를 낮춥니다.
- 기준일, 이벤트, 프로모션은 세로선·점·짧은 라벨로 표시합니다.
- 축에는 단위와 기간을 명시하고, 색상만으로 선의 의미를 구분하지 않습니다.
- 차트가 핵심 판단을 대신하지 않도록 같은 데이터를 표나 요약 문구로도 제공합니다.

## 9. 아이콘과 이미지

- 아이콘은 의미가 명확한 단순 선형 아이콘을 사용합니다.
- 장식용 아이콘은 텍스트보다 강하게 보이지 않게 합니다.
- 이미지가 업무 정보를 전달하지 않으면 사용하지 않습니다.
- 외부 이미지 URL이나 임의의 프로필 이미지를 제품 화면에 직접 의존하지 않습니다.
- 아이콘 버튼에는 접근 가능한 이름을 반드시 제공합니다.

## 10. 접근성

- 본문과 배경의 대비를 충분히 확보합니다.
- 상태는 색상 외에 텍스트로도 전달합니다.
- 키보드 포커스는 `outline` 또는 블루 링으로 명확히 보이게 합니다.
- 표의 헤더와 데이터 관계를 유지하고, 모바일에서도 열 의미가 사라지지 않게 합니다.
- 폼 입력에는 연결된 `label`이 있어야 합니다.
- 버튼·링크·입력의 터치 영역은 모바일에서 최소 44px을 목표로 합니다.
- 애니메이션은 짧고 기능 피드백에 한정하며, 정보 이해를 방해하지 않습니다.

## 11. 문구 규칙

- 모든 사용자에게 보이는 문구와 주석은 한국어로 씁니다.
- 제목은 짧은 명사형, 설명은 목적과 기준이 드러나는 문장형으로 씁니다.
- 영문 상태값은 필요한 경우 한국어 뜻을 함께 표시합니다. 예: `CRITICAL · 위험`.
- “데이터 없음”과 “조회 실패”를 구분합니다.
- 확정되지 않은 값을 확정된 것처럼 표현하지 않습니다.
- “AI가 판단했다”보다 사용한 기준과 영향 범위를 설명합니다.

## 12. 화면 구현 순서

새 화면 또는 기존 화면 개편은 아래 순서로 진행합니다.

1. 페이지 목적과 핵심 판단을 한 문장으로 정의합니다.
2. `analysis-frame`과 기존 공통 레이아웃을 적용합니다.
3. KPI와 상태 배지를 먼저 배치합니다.
4. 표 또는 핵심 분석 영역을 배치합니다.
5. 조회 실패·빈 결과·계산 불가 상태를 구현합니다.
6. 필요한 경우 보조 차트와 설명 카드를 추가합니다.
7. 1050px, 760px 기준의 반응형을 확인합니다.
8. 실제 데이터 건수와 대표 행 하나를 손으로 검증합니다.
9. `npm run build`로 빌드합니다.

## 13. 금지 규칙

- 새 CSS 프레임워크나 새 UI 라이브러리를 추가하지 않습니다.
- 화면 컴포넌트에서 평균, 분위수, 리드타임, 소진일을 계산하지 않습니다.
- `raw` 스키마를 화면에서 직접 조회하지 않습니다.
- 없는 데이터를 임의로 만들어 목업처럼 표시하지 않습니다.
- 계산 불가 값을 0이나 999로 채우지 않습니다.
- 한 화면에 강한 색상, 그라데이션, 그림자, 애니메이션을 동시에 과도하게 사용하지 않습니다.
- 긴 설명을 배지·버튼·탭 안에 넣지 않습니다.
- 모바일에서 표를 카드 목록으로 바꾸면서 열의 의미를 잃게 만들지 않습니다.

## 14. 적용 우선순위

전체 시스템에 적용할 때는 다음 순서로 진행합니다.

1. `app/globals.css`의 토큰과 공통 레이아웃을 정리합니다.
2. `components/analysis/analysis-frame.tsx`와 `data-table.tsx`를 기준 화면으로 맞춥니다.
3. `app/analysis/leadtime/page.tsx`와 `app/analysis/stockout/page.tsx`에 동일한 상태·표·KPI 규칙을 적용합니다.
4. `components/workflow/` 화면을 같은 토큰과 버튼·카드·상태 규칙으로 맞춥니다.
5. 마지막으로 모바일과 빈 상태를 점검합니다.

이 문서와 `AGENTS.md`, `SCHEMA.md`의 규칙이 충돌할 경우 `AGENTS.md`와 `SCHEMA.md`를 우선합니다.
