# 4회차 준비 커밋 적용 방법

이 문서는 현재 `superSCM` 저장소에 4회차 준비 커밋을 적용할 때 사용하는 안내서입니다.

## 먼저 확인할 사항

현재 저장소에는 Phase 2 예제 구현이 포함되어 있습니다. 다른 저장소에 적용할 때 아래 파일이 빠져 있으면 이 저장소의 파일을 먼저 복사합니다.

```text
AGENTS.md
SCHEMA.md
lib/supabase.ts
lib/scm.ts
components/analysis/analysis-frame.tsx
components/analysis/data-table.tsx
app/analysis/leadtime/page.tsx
docs/04-실습안내.md
```

이 파일들은 빈 파일로 만들지 않습니다. 분석 화면과 조회 함수의 원본을 함께 복사합니다.

## 1. 준비 커밋 파일 복사

준비 커밋 폴더의 내용을 저장소 루트에 복사합니다. `<준비커밋_폴더>`는 강사가 받은 실제 폴더 경로로 바꿉니다.

```bash
cd ~/superSCM
cp -r <준비커밋_폴더>/* .
cp <준비커밋_폴더>/.env.local.example .
cp <준비커밋_폴더>/.gitignore .
```

복사 후 `package.json`에 `@supabase/supabase-js`가 있는지 확인합니다.

```bash
npm install
```

## 2. Supabase를 수업 전날 준비

강사가 한 번만 실행합니다. 참가자는 CSV를 다시 임포트하지 않습니다.

1. SQL Editor에서 `04_setup.sql`의 STEP A만 실행합니다. `→ 여기서 CSV를 임포트하고` 문장 앞까지 실행합니다.
2. Table Editor에서 CSV 6개를 원본 그대로 임포트합니다.
   - `supplier_master.csv` 13행: 기존 테이블 교체
   - `item_master.csv` 23행: 기존 테이블 교체
   - `purchase_order.csv` 92행: 기존 테이블 교체
   - `inventory.csv` 43행: 기존 테이블 교체
   - `shipment_log.csv` 2,864행: 새 빈 테이블에 임포트
   - `usage_history.csv` 7,038행: 새 빈 테이블에 임포트
3. CSV를 엑셀로 열었다가 저장하지 않습니다. 날짜 형식이 바뀌면 임포트가 실패합니다.
4. `01~03` SQL을 실행하고 아래 건수를 확인합니다.
5. `04_analytics.sql` 전체를 실행합니다.
6. Supabase → Settings → API → Exposed schemas에 `public`, `core`, `analytics`를 추가합니다.

확인 쿼리:

```sql
select count(*) from raw.shipment_log;      -- 2864
select count(*) from raw.usage_history;    -- 7038
select count(*) from raw.inventory;         -- 43
select count(*) from raw.item_master;       -- 23
select count(*) from raw.supplier_master;   -- 13
select count(*) from core.supplier_alias;   -- 34
select count(*) from core.leadtime_plan;    -- 0
select count(*) from core.usage_profile;    -- 0
select * from analytics.v_stockout_kpi;
```

`leadtime_plan`과 `usage_profile`은 수업 오전에 참가자와 함께 채울 테이블이므로 준비 단계에서는 반드시 0이어야 합니다.

## 3. 강사 준비 커밋 검증

`.env.local`에 실제 Supabase URL과 키를 넣고 실행합니다.

```bash
cp .env.local.example .env.local
npm run dev
```

다음 화면을 확인합니다.

```text
/                         워크플로우 6단계와 ANALYSIS 메뉴
/analysis/leadtime        공급처 12행, 격차 전부 양수
/analysis/po-risk         404 (정상)
```

## 4. 커밋과 수강생 안내

검증이 끝난 뒤 강사가 커밋하고 push합니다.

```bash
git add .
git commit -m "Phase 2: Supabase 연결 및 리드타임 분석 예제 화면 추가"
git push origin main
```

수강생은 수업 당일 아래만 실행합니다.

```bash
git pull origin main
npm install
cp .env.local.example .env.local
# 강사가 알려주는 URL과 키 입력
npm run dev
```

수업 시작에는 강사와 함께 Supabase 건수, Exposed schemas, `/analysis/leadtime` 12행을 확인합니다.

## 현재 저장소 상태

현재 저장소에는 Phase 1 화면과 Phase 2 리드타임 분석 예제가 함께 있습니다. `npm run build`가 통과하면 `/analysis/leadtime` 라우트가 생성됩니다. 실제 데이터가 나오려면 Supabase에 `analytics.v_leadtime_gap` 뷰가 있고 `public`, `core`, `analytics` 스키마가 노출되어 있어야 합니다.

## 5. STEP 4 파일 적재 파이프라인 적용

STEP 4는 기존 RAW 테이블을 삭제하거나 재생성하지 않고, 추가 migration으로 업로드 이력·staging·검증 오류·rollback 백업 구조를 구성합니다.

1. `supabase/migrations/20260828000300_create_import_pipeline.sql` 전체를 Supabase SQL Editor에서 실행합니다.
2. 아래 테스트 SQL을 실행해 객체와 권한을 확인합니다.

```text
supabase/tests/import-pipeline.sql
supabase/tests/import-rpc.sql
```

3. Supabase → Settings → API → Exposed schemas에서 `core`, `analytics`가 노출되어 있는지 확인합니다. `raw`는 브라우저 조회용으로 노출하지 않습니다.
4. 의존성 설치 후 개발 서버를 시작합니다.

```bash
npm install
npm run dev
```

5. ADMIN 계정으로 `/admin/data-management`에 접속합니다.
   파일 선택 → 타입/모드 선택 → Parse & Preview → 컬럼 매핑 확인 → Validation → 오류 확인 → Import 순서로 진행합니다.
6. `replace`는 기존 데이터를 백업한 뒤 교체하므로 확인 체크 없이는 실행되지 않습니다. 운영 데이터에서는 먼저 작은 검증 파일로 `append`를 시험합니다.
7. Import 후 Import History에서 `batch_id`, 행 건수, stale 상태를 확인합니다. 오류 또는 경고 행은 화면의 Error CSV로 내려받을 수 있습니다.
8. 잘못 적재한 batch만 rollback합니다. `replace` rollback은 백업이 존재하는 경우에만 기존 데이터를 복원합니다.

적재 파이프라인 migration이 적용되기 전에는 `/admin/data-management`에서 `upload_batch` 조회 오류가 발생하는 것이 정상이며, migration 적용 후 서버를 재시작해야 합니다.

## 6. STEP 5 SKU Demand Profile 적용

1. STEP 3의 `core.forecast_setting`에 학습 시작일과 종료일이 설정되어 있는지 확인합니다.
2. `supabase/migrations/20260828000400_create_demand_profile.sql` 전체를 SQL Editor에서 실행합니다.
3. `supabase/tests/demand-profile.sql`을 실행합니다.
4. Settings → API → Exposed schemas에 `analytics`와 `core`가 포함되어 있는지 확인합니다.
5. 개발 서버를 재시작한 뒤 `/analysis/demand-profile`로 이동합니다.

Demand Profile은 `core.v_train_demand`의 학습기간만 사용합니다. 월별 기간 Grid에서 실제 기록이 없는 월은 기간상 무수요로 0을 만들지만 원본의 null 수량을 보정하지 않습니다. Peak month가 동률이면 가장 이른 월을 사용합니다. 24개월 미만은 seasonality를 `null + INSUFFICIENT_PERIODS`로 표시하며 `false`로 바꾸지 않습니다.

## 7. STEP 6 Forecast Engine 적용

1. STEP 3~5 migration과 Demand Profile view가 적용된 상태에서 `supabase/migrations/20260828000500_create_forecast_engine.sql` 전체를 SQL Editor에서 실행합니다.
2. `supabase/tests/forecast-engine.sql`을 실행해 다섯 개 모델, 권한, Snapshot, 결과 interval, train view 참조 여부를 확인합니다.
3. `/admin/forecast-models`에서 사용할 Baseline 모델의 Enabled 상태와 적용 Demand Type을 확인합니다.
4. `/admin/forecast-runs`에서 `Baseline Forecast 실행`을 누릅니다. 실행 함수는 `core.forecast_setting.forecast_horizon`과 활성 모델을 사용합니다.
5. 실행이 성공하면 `/admin/forecast-runs`에서 `SUCCESS`, 모델 수, SKU 수, 결과 행 수, `FRESH`를 확인합니다.
6. 모델을 바꾸거나 수요성 데이터를 Import하면 과거 결과는 삭제되지 않고 기존 Run이 `STALE`로 표시됩니다. 과거 결과를 재현할 때는 `core.model_version` Snapshot과 해당 `run_id`를 함께 사용합니다.

STEP 6 migration 적용 전에는 Forecast 모델/실행 화면에서 view 또는 RPC 조회 오류가 발생할 수 있습니다. Forecast 실행은 ADMIN 전용이며, 데이터가 부족한 모델 결과는 `reason_code`와 null interval로 남습니다.
